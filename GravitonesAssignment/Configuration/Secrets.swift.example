//
//  Secrets.swift.example
//  GravitonesAssignment > Configuration
//
//  Template for the gitignored `Secrets.swift`.
//  To run the project:
//    1. Copy this file to `Secrets.swift` in the same folder.
//    2. Fill in the base URL shared with you in the assignment brief.
//
//  `Secrets.swift` is gitignored so real config never lands in source control.
//

import Foundation

enum Secrets {
    /// Base URL of the HLS Interview Platform API. No trailing slash.
    static let apiBaseURL = "http://<base-url-here>"
}
